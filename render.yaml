services:
  - type: web
    name: pt-quana-raya-shakatama
    env: php
    region: singapore
    plan: free
    buildCommand: |
      composer install --optimize-autoloader --no-dev
      php artisan config:cache
      php artisan route:cache
      php artisan view:cache
    startCommand: "heroku-php-apache2 public/"